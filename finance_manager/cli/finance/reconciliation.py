import click


@click.pass_obj
def reconcilefee(config):
    """
    Reconcile HE income in sets to QL. 

    Intended to check that figures presented for a given ACAD_YEAR SET_CAT reconcile to those in QL.  

    Parameters
    ----------
    config : Config
        Custom config object. 
    """
    pass
